import React from "react"

const NavBar = () => {

  return(
    <div className="default-primary-color nav-container">
      <a className="accent-color nav-btn" href="#about">About</a>
      <a className="accent-color nav-btn" href="#projects">Projects</a>
      <a className="accent-color nav-btn" href="#contact">Contact</a>
    </div>
  )
}

export default NavBar